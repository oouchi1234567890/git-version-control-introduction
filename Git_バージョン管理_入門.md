# Git バージョン管理 入門

**ローカル環境で、変更を確認し、意図を持って記録できるプログラマーへ**

本教材は[『Gitバージョン管理_入門_公開用_v1.1.pptx』](slides/Gitバージョン管理_入門_公開用_v1.1.pptx)をもとに、初めてGitを学ぶプログラマー向けに再構成したものです。原資料の主要テーマを押さえながら、ローカルで実行できる演習と、実務につながる確認の習慣を補っています。

Gitを使う目的は、コマンドを暗記することだけではありません。自分が加えた変更を説明でき、必要な変更だけを履歴として残せることが大切です。

[English version](Git_Version_Control_Introduction_EN.md)

## 学習のゴール

学習後は、次のことを自分の言葉で説明し、操作できる状態を目指します。

- GitとGitHubの役割を区別できる。
- ワーキングツリー、インデックス、リポジトリの関係を説明できる。
- ファイルの変更を確認して、適切な単位でコミットできる。
- ブランチを作成し、変更をmainへマージできる。
- コンフリクトの内容を読み、修正して統合を完了できる。

**演習環境：WindowsのGit Bash。** ファイルの編集には使い慣れたテキストエディタを利用します。演習は自分のPCだけで完結し、GitHubアカウントは不要です。コードブロックのコマンドを上から順番に実行してください。`<ファイル名>` などの山括弧は説明用の置き換え箇所です。

## 1. バージョン管理を仕事の基礎にする

![変更履歴を表す原資料のアイコン](images/history.png)

### 1.1 バージョン管理とは

バージョン管理は、ファイルの変更履歴を記録し、後から比較・確認できるようにする仕組みです。プログラムを修正したとき、「どこが変わったか」「誰が記録したか」「なぜ変更したか」を追えるようにします。

例えば、不具合修正の後に別の問題が見つかった場合、修正前後の差分を調べることで原因を探せます。必要に応じて、記録済みの状態を取り出すこともできます。

`report_v1`、`report_final`、`report_final2` のようにファイル名で版を増やしていくと、どれが正しい状態なのか判断しにくくなります。Gitでは同じファイル名を使いながら、変更の区切りを履歴として残せます。

> **仕事につながる考え方**：未来の自分や同僚が、変更内容とその理由を読み取れる履歴を残しましょう。「なぜ変えたか」はGitが自動で判断する情報ではないため、コミットメッセージで伝えます。

Gitで確実に取り出せるのは、基本的に記録してある内容です。保存しただけの変更や未追跡ファイルが、すべて自動でバックアップされるわけではありません。

### 1.2 GitとGitHubの違い

| 比較項目 | Git | GitHub |
| --- | --- | --- |
| 役割 | ファイルの変更履歴を管理するソフトウェア | Gitリポジトリを保管・共有し、共同作業を支えるサービス |
| 主な利用場所 | 自分のPCやサーバー | インターネット上のサービス |
| 代表的な操作 | 記録、差分確認、ブランチ作成、統合 | リポジトリ共有、変更レビュー、課題管理 |
| ローカル学習での必要性 | 必要 | 不要 |

Gitで作成したコミットは、まず自分のPCに記録されます。GitHubなどと共有するときに、別途送信操作を行います。

### 1.3 集中型と分散型

| 方式 | 履歴の持ち方 | 通信できないとき | 代表例 |
| --- | --- | --- | --- |
| 集中型 | 中央サーバーが履歴を管理する | ファイル編集はできても、サーバーへのコミットなどは制限される | Subversion（SVN）、CVS |
| 分散型 | 各利用者が手元にもリポジトリを持つ | 手元でコミットや履歴確認ができる | Git、Mercurial |

Gitは、一人でローカルだけで利用していても分散型です。通常のクローンでは履歴も取得しますが、履歴の範囲を制限する取得方法もあります。

### 1.4 Gitの特徴

Gitは2005年にLinuxカーネルの開発を背景として生まれた、オープンソースのバージョン管理システムです。Windows、macOS、Linuxで利用できます。

履歴の記録や差分確認など、多くの操作がローカルで完結します。また、ブランチを使うことで、本流から作業を分けて変更を試せます。

Gitはコミット時点のファイル群の状態を、スナップショットとして扱います。毎回すべてのファイルを単純に重複保存するという意味ではなく、変更のない内容を再利用するなど、内部では効率よく管理します。参考：[Git公式書籍「What is Git?」](https://git-scm.com/book/en/v2/Getting-Started-What-is-Git%3F)

## 2. 最初に理解する3つの場所

### 2.1 編集・選択・記録

| 場所 | 役割 | README.mdを変更した例 |
| --- | --- | --- |
| ワーキングツリー | 実際にファイルを編集する場所 | エディタで説明を書き足して保存する |
| インデックス（ステージングエリア） | 次のコミットに含める状態を準備する場所 | `git add README.md` で内容を登録する |
| ローカルリポジトリ | コミットや履歴を保管する場所 | `git commit` で記録する |

```text
ワーキングツリー      インデックス       ローカルリポジトリ
  編集・保存      →     内容を選ぶ    →       履歴に残す
                 git add           git commit
```

通常のリポジトリでは、履歴や設定などをプロジェクト内の `.git` ディレクトリで管理します。演習中は `.git` の中身を直接編集・削除しないでください。

### 2.2 保存、add、commitはそれぞれ別の操作

エディタの保存は、作業中のファイルを更新します。`git add` は、その時点の内容を次のコミット用に登録します。`git commit` は、登録した内容を履歴として残します。

**add後にさらに編集しても、その追加編集は自動でステージされません。** 同じファイルに「ステージ済み」と「未ステージ」の変更が同時に存在することがあります。次のコミットに含めたい場合は、差分を確認して再度addします。

### 2.3 statusで状態を読む

```bash
git status
```

| 表示 | 意味 | 次の判断 |
| --- | --- | --- |
| `Untracked files` | まだ追跡していない新しいファイル | 管理対象ならaddする |
| `Changes not staged for commit` | 追跡中のファイルに未ステージの変更がある | diffで内容を確認する |
| `Changes to be committed` | 次のコミットに含める変更がある | stagedの差分を確認する |
| `nothing to commit, working tree clean` | 通常のstatusで報告する未記録の変更がない | 次の作業へ進める |

`git status` は実行した時点の状態を表示します。常時監視するために起動したままにするコマンドではありません。

### 2.4 diffの比較対象

![差分を確認する原資料のアイコン](images/diff.png)

| コマンド | 比較対象 | 主に確認すること |
| --- | --- | --- |
| `git diff` | インデックスとワーキングツリー | add前の変更 |
| `git diff --staged` | HEADとインデックス | 次のコミットに入る変更 |
| `git diff HEAD` | HEADとワーキングツリー | 最後のコミットからの作業ファイルの変化 |
| `git diff <コミットA> <コミットB>` | 指定した2つのコミット | 記録済みの状態の違い |

`HEAD` は通常、現在のブランチの先端コミットを指します。最初のコミットを作る前は、比較できるHEADのコミットはありません。

通常の `git diff` は未追跡ファイルの内容を表示しません。新規ファイルはエディタで確認し、add後に `git diff --staged` で確認できます。差分表示では、行頭の `-` が削除、`+` が追加を表します。

## 3. Gitのインストールと初期設定

### 3.1 インストールを確認する

[Git公式サイト](https://git-scm.com/)から、自分のOSに対応する導入案内を確認します。WindowsではGit for WindowsをインストールしてGit Bashを開きます。会社のPCでは、指定された導入方法や設定がある場合はそれに従ってください。

```bash
git --version
```

`git version ...` と表示されれば、コマンドを実行できています。本教材は `git switch`、`git restore`、`git init -b main` が使えるGit 2.28以降を前提とします。

### 3.2 記録者情報を設定する

以下の名前とメールアドレスは練習用です。実務では、組織が指定する情報に置き換えてください。

```bash
git config --global user.name "Git Learner"
git config --global user.email "learner@example.com"
```

この情報はコミットに記録されます。GitHubへのログイン設定ではありません。`--global` は、そのPCの現在のユーザーに共通する設定です。

設定を確認します。

```bash
git config --global --get user.name
git config --global --get user.email
```

プロジェクトごとに情報を変える場合は、リポジトリ内で `--global` を付けずに設定します。そのリポジトリではローカル設定が優先されます。

```bash
git config user.name "Project Developer"
git config user.email "developer@example.com"
```

上のローカル設定例は、後の演習でリポジトリを作成してから利用できる補足例です。すべての例を実行する必要はありません。

Windowsのグローバル設定ファイルは通常 `C:\Users\ユーザー名\.gitconfig` にあります。`git config --list --show-origin` で設定と読み込み元を確認できます。

`git config --global --unset user.name` は、グローバル設定の `user.name` だけを削除します。設定全体を削除する操作ではありません。

## 4. 演習：初めてのコミット

### 4.1 練習専用のフォルダを作る

Git Bashで次を実行します。すでに同名のフォルダがある場合は、新しい名前に変更してください。既存の業務プロジェクトの中では実行しません。

```bash
cd ~
mkdir git-local-practice
cd git-local-practice
git init -b main
git status
```

`git init` は、新しいローカルリポジトリを初期化します。`-b main` は最初のブランチ名をmainに指定します。`No commits yet` と表示される段階では、まだコミットはありません。

### 4.2 README.mdを作成する

エディタで練習フォルダを開き、`README.md` をUTF-8で保存します。内容は次のとおりです。

```markdown
# Git Local Practice

Gitのローカル操作を学ぶための練習用リポジトリです。
```

Git Bashに戻って確認します。

```bash
git status
```

`README.md` が `Untracked files` に表示されれば、新規ファイルをGitが見つけています。

### 4.3 ステージしてから内容を確認する

```bash
git add README.md
git status
git diff --staged
```

`Changes to be committed` に `new file: README.md` が表示されます。差分で、作成した本文が追加されることを確認してください。

原資料にある `git add .` は、現在のディレクトリ以下の変更をまとめてステージします。最初はファイル名を指定して、何を登録するか意識する練習をしましょう。

### 4.4 最初のコミットを作る

```bash
git commit -m "練習用READMEを追加"
git status
git log --oneline
```

`-m` はコミットメッセージを指定するオプションです。成功すると、ログに短いコミットIDとメッセージが表示されます。コミットIDは環境ごとに異なります。

確認する点は、ログにメッセージがあることと、statusがcleanになっていることです。これはPC内に履歴を作成した状態です。

### 4.5 変更を加えて2回目のコミットを作る

README.mdの末尾に、次の内容を追記して保存します。

```markdown

## 学習内容

- 変更を確認してコミットする
```

```bash
git status
git diff
git add README.md
git diff --staged
git commit -m "READMEに学習内容を追記"
git log --oneline
```

ログに2件のコミットが表示されれば完了です。`git log` や差分表示から戻れないときは、ページャーが開いている場合があります。`q` キーで終了できます。

### 4.6 プロとして身に付けたいコミットの習慣

変更は、後から説明できるまとまりで記録します。例えば、不具合修正と無関係な見た目の変更を同じコミットに詰め込むと、レビューや原因調査が難しくなります。

| 曖昧なメッセージ | 内容が伝わるメッセージ |
| --- | --- |
| 修正 | READMEに起動手順を追記 |
| バグ対応 | 空の入力で計算処理が停止する問題を修正 |
| 更新 | 学習内容にブランチ操作を追加 |

コミット前には、差分に意図しない削除や不要なファイルがないか確認します。プログラムを変更した場合は、動作確認や必要なテストも行います。

## 5. 演習：ブランチで作業を分ける

ブランチは、開発の履歴に名前を付け、作業の流れを分ける仕組みです。本教材では本流を `main`、説明追加用のブランチを `feature/readme-guide` とします。

### 5.1 作成と切り替え

第4章を完了し、作業ツリーがcleanになっていることを確認して実行します。

```bash
git status
git switch -c feature/readme-guide
git branch
```

`-c` は作成して切り替える指定です。`git branch` では、現在のブランチに `*` が付きます。

| 目的 | 本教材で使うコマンド | 原資料にある対応コマンド |
| --- | --- | --- |
| ブランチだけを作成 | `git branch feature/readme-guide` | 同じ |
| 既存ブランチへ移動 | `git switch main` | `git checkout main` |
| 作成して移動 | `git switch -c feature/readme-guide` | `git checkout -b feature/readme-guide` |

この表は対応関係の説明です。すでに作成したブランチを、もう一度作成する必要はありません。未コミットの変更は切り替え先にも残ったり、切り替えを妨げたりするため、区切りのよい状態でコミットしてから移動します。

### 5.2 作業ブランチで変更を記録する

README.mdの「学習内容」に次の項目を追加します。

```markdown
- ブランチで作業を分けてマージする
```

```bash
git diff
git add README.md
git diff --staged
git commit -m "学習内容にブランチ操作を追加"
git switch main
```

mainに戻った後、README.mdをエディタで開き直すと、今追加した項目はまだありません。コミットが作業ブランチに記録されているためです。

### 5.3 mainへ統合する

```bash
git merge feature/readme-guide
git log --oneline --graph --all
git status
```

`git merge` は、**現在いるブランチへ**指定したブランチの変更を取り込みます。今回はmainに切り替えてから実行しているため、mainに変更が入ります。

この演習では分岐後にmainへ新しいコミットを加えていないため、通常は `Fast-forward` と表示されます。mainの指す位置を作業ブランチの先端まで進める統合で、新しいマージコミットは作りません。

```text
統合前： A──B  main
            └──C  feature/readme-guide

統合後： A──B──C  main, feature/readme-guide
```

確認後、統合済みの作業ブランチ名を削除できます。

```bash
git branch -d feature/readme-guide
```

統合済みのコミットはmainの履歴に残ります。

## 6. マージとリベースを理解する

### 6.1 履歴が分岐した場合のマージ

mainと作業ブランチの両方に独自のコミットがある場合、通常のマージでは、両方の履歴をつなぐマージコミットを作ります。

```text
A──B──C────M  main
    └──D──┘
```

`M` がマージコミットです。分岐と統合の経緯が履歴に残ります。同じ箇所を別々に変更している場合などは、コンフリクトが起きることがあります。

### 6.2 リベースはコミットを別の土台に載せ直す

リベースは、作業ブランチの変更を別の先端から順に適用し直す操作です。次は概念説明用で、第5章の続きとして実行するコマンドではありません。

```bash
git switch feature/example
git rebase main
```

```text
実行前： A──B──C  main
            └──D──E  feature/example

実行後： A──B──C  main
               └──D'──E'  feature/example
```

`D'` と `E'` は、変更を適用し直して作成したコミットです。コミットIDが変わります。main自体はこの操作だけでは先へ進みません。

| 観点 | merge | rebase |
| --- | --- | --- |
| 元の履歴 | 元のコミットを保って統合する | 対象コミットを別の土台で作り直す |
| 統合後の形 | 分岐を残す場合がある | 作業履歴を直線状にできる |
| コミット | Fast-forwardでは追加なし。通常の分岐統合ではマージコミットを作る | 適用し直したコミットを作る。内容によって省略される場合もある |
| 入門での扱い | まず確実に操作できるようにする | 意味を理解し、未共有の作業ブランチで学ぶ |

共有済みの履歴を書き換えると、同僚の履歴との整合が崩れることがあります。共有ブランチのリベースは、チームの運用を確認してから行います。

## 7. 演習：コンフリクトを解決する

コンフリクトは、Gitが自動で統合内容を決められない状態です。一人のローカル作業でも、別ブランチで同じ箇所を変更すれば発生します。

### 7.1 同じ行に異なる変更を作る

第5章を完了した練習リポジトリで、mainにいることとcleanであることを確認します。

```bash
git switch main
git status
```

エディタで `message.txt` を作り、次の1行を保存します。

```text
こんにちは
```

```bash
git add message.txt
git commit -m "挨拶メッセージを追加"
git switch -c feature/greeting
```

feature/greeting側で、message.txtの1行を置き換えて保存します。

```text
こんにちは、Gitを学びましょう。
```

```bash
git add message.txt
git commit -m "挨拶に学習案内を追加"
git switch main
```

main側で同じ1行を、次の内容に置き換えて保存します。

```text
こんにちは、開発チームへようこそ。
```

```bash
git add message.txt
git commit -m "挨拶にチームへの歓迎を追加"
git merge feature/greeting
```

この演習では、同じ行の変更が競合するため `CONFLICT` が表示されます。

### 7.2 マーカーを読み、完成形を決める

```bash
git status
```

message.txtを開くと、通常は次の形式のマーカーが入っています。設定によっては共通の変更元も表示されます。

```text
<<<<<<< HEAD
こんにちは、開発チームへようこそ。
=======
こんにちは、Gitを学びましょう。
>>>>>>> feature/greeting
```

このマージでは、上側が現在のmain、下側が取り込むfeature/greetingの内容です。「自分と他人」の区別ではありません。

両方の意図を読み取り、ファイル全体を次の1行に直して保存します。`<<<<<<<`、`=======`、`>>>>>>>` のマーカーも削除します。

```text
こんにちは、開発チームへようこそ。Gitを学びましょう。
```

### 7.3 解決結果を記録する

```bash
git add message.txt
git diff --staged
git status
git commit -m "挨拶の競合を解決し歓迎と学習案内を統合"
git log --oneline --graph --all
git status
```

完成した内容が正しいことを確認し、cleanになれば完了です。プログラムの競合では、マーカーを消すだけでなく、統合後の動作も確認してください。

進行中のマージを取りやめる場合は `git merge --abort` を使います。これは完了済みマージを取り消すコマンドではありません。

リベース中の競合では、修正してaddした後に `git rebase --continue` で処理を進めます。取りやめる場合は `git rebase --abort` を使います。マージとリベースでは、解決後に続行する操作が異なります。

## 8. 間違えたときの基本対応

### 8.1 ステージだけを取り消す

最初のコミットがあるリポジトリで、README.mdを誤ってステージした場合の例です。

```bash
git restore --staged README.md
```

ワーキングツリーの編集内容は残ります。次のコミットからいったん外して、再確認したいときに使います。

### 8.2 未ステージの編集を破棄する

```bash
git diff -- README.md
git restore README.md
```

`git restore README.md` は、作業ファイルをインデックスの内容へ戻し、未ステージの編集を破棄します。**残したい編集があるときは実行しないでください。** コミットしていない内容は、Gitで復元できるとは限りません。

### 8.3 よくある表示への対応

| 表示・状況 | 確認すること |
| --- | --- |
| `git: command not found` | インストール後にGit Bashを開き直し、導入状態を確認する |
| `not a git repository` | `pwd` で現在地を確認し、練習リポジトリへ `cd` する |
| `Author identity unknown` | user.nameとuser.emailを確認する |
| `nothing added to commit` | 保存したか、対象をaddしたか、statusで確認する |
| `git diff` に新規ファイルが出ない | 未追跡ファイルはエディタで読み、add後にstagedの差分を確認する |
| ブランチを切り替えられない | 未コミットの変更を確認し、必要な変更をコミットする |
| コミット時にエディタが開いた | `-m` なしではメッセージ入力用エディタが開く。教材では `-m` を使う |

状態が分からなくなったら、まず `git status` を読みます。強制的な削除や履歴の書き換えで進めようとせず、何が残っているかを確かめましょう。

## 9. 実務への補足：管理しないファイル

`.gitignore` は、未追跡ファイルのうち、通常の追跡対象から除外したいものを指定するファイルです。プロジェクトのルートに作成します。

```gitignore
# ローカル設定・秘密情報
.env

# 生成物やログ
build/
*.log
```

除外内容は、利用言語やチームのルールに合わせます。`.gitignore` 自体は通常コミットして共有します。

すでに追跡しているファイルは、`.gitignore` に書くだけでは追跡が止まりません。また、履歴中の情報が消えるわけでもありません。APIキーやパスワードを含むファイルは、最初のadd前に確認しましょう。

## 10. リモート連携の予備知識

ここは原資料のリモート連携を整理した補足です。ローカル演習では実行しません。

リモートリポジトリは、共有先などとして登録する別のリポジトリです。GitHub、GitLab、Bitbucketなどのサービスに置くことができます。

| コマンド例 | 役割 |
| --- | --- |
| `git clone <URL>` | リポジトリを取得し、ローカルで作業を始める |
| `git fetch origin` | リモートの履歴情報を取得する |
| `git push origin main` | ローカルmainのコミットを送信し、リモート側のmainを更新する |
| `git pull --ff-only` | 取得した更新をFast-forwardできる場合だけ取り込む |

`origin` は共有先の登録名としてよく使われる名前です。実行にはリモートや追跡先、必要な認証の設定が前提となります。

**pushが送るのはコミット済みの履歴です。** ステージに置いただけの変更や未コミットの編集は送信されません。参考：[git-push公式リファレンス](https://git-scm.com/docs/git-push)

pullは取得と統合を行います。統合方法はオプションや設定により異なり、マージやリベースなどを使います。`--ff-only` は履歴が分岐していたら停止する指定です。停止したときは履歴とチームの方針を確認します。参考：[git-pull公式リファレンス](https://git-scm.com/docs/git-pull)

`checkout`、`switch`、`diff` はローカルでも使うコマンドです。リモート通信のためのコマンドとして覚える必要はありません。

## 11. 操作を助けるツール

原資料では次のツールを紹介しています。

| ツール | 主な役割 |
| --- | --- |
| Git Bash | WindowsでGitやシェルのコマンドを実行する環境 |
| Git GUI | ステージやコミットなどを画面で操作するツール |
| Sourcetree | 履歴やブランチを視覚的に確認するGitクライアント |
| TortoiseGit | WindowsエクスプローラーからGit操作を行うクライアント |

エディタやGUIを使う場合も、操作が「編集」「ステージ」「コミット」のどれに当たるのかを意識します。本教材では、状態を言葉で理解するためにGit Bashで操作します。

## 12. コマンド早見表

| やりたいこと | コマンド |
| --- | --- |
| バージョンを確認する | `git --version` |
| 新しいリポジトリを作る | `git init -b main` |
| 状態を確認する | `git status` |
| 未ステージの差分を見る | `git diff` |
| コミット予定の差分を見る | `git diff --staged` |
| 指定ファイルをステージする | `git add <ファイル名>` |
| メッセージ付きで記録する | `git commit -m "変更内容"` |
| 履歴を短く表示する | `git log --oneline` |
| 分岐を含む履歴を見る | `git log --oneline --graph --all` |
| ブランチを一覧表示する | `git branch` |
| ブランチを作って移動する | `git switch -c <ブランチ名>` |
| 既存ブランチへ移動する | `git switch <ブランチ名>` |
| 現在のブランチへ統合する | `git merge <ブランチ名>` |
| 統合済みブランチを削除する | `git branch -d <ブランチ名>` |
| ステージを取り消す | `git restore --staged <ファイル名>` |
| 進行中のマージを取りやめる | `git merge --abort` |

## 13. 理解度チェック

1. ファイルを保存しただけで、Gitの履歴に残りますか。
2. add後にさらに編集した場合、commitにはどの状態が入りますか。
3. 次のコミットに入る変更は、どのコマンドで確認しますか。
4. featureの変更をmainへ取り込むには、どのブランチでmergeしますか。
5. Fast-forwardでは、新しいマージコミットを作りますか。
6. コンフリクトのマーカーを消した後、何を確認してから記録しますか。
7. addしただけの内容をpushで共有できますか。

### 解答例

1. 残りません。ステージしてコミットする必要があります。
2. 最後にaddした時点の内容です。後の編集を含めるには、再度addします。
3. `git diff --staged` です。
4. mainへ切り替えてから `git merge <作業ブランチ名>` を実行します。
5. 作りません。取り込み先のブランチの位置を先へ進めます。
6. 統合した内容の意味と動作が正しいことを確認し、addしてコミットします。
7. できません。まずコミットする必要があります。

### 修了時の確認

- [ ] 3つの管理領域を説明できる。
- [ ] READMEの追加と変更を、別々のコミットに記録できた。
- [ ] 作業ブランチの変更をmainへ取り込めた。
- [ ] 競合を読み、意図した内容に直して統合できた。
- [ ] コミット前に差分を確認する理由を説明できる。

日々の作業では、編集後にstatusで状況を確認し、diffで内容を読み、必要な変更をaddします。最後にステージ済みの差分を確認してコミットします。この手順を、自分の変更に責任を持つための基本動作として身に付けましょう。

## 付録：原資料との対応・利用条件

### 原資料との対応

| 原資料のスライド | 本教材で扱う内容 |
| --- | --- |
| 1〜12 | 学習のゴール、第1章：バージョン管理、GitとGitHub、集中型・分散型、Gitの特徴 |
| 13〜18 | 第2章・第4章：3つの管理領域、状態確認、ステージ、コミット |
| 19〜22 | 第2章・第10章：差分確認、リモート連携、checkoutとの対応 |
| 23〜28 | 第5〜6章：ブランチ、Fast-forward、マージ、リベース |
| 29〜30 | 第7章：コンフリクト |
| 31〜32 | 第11章：ツール・クライアント |
| 33〜38 | 第3〜5章・第12〜13章：初期設定、実践、振り返り |
| 39 | 以下の著作権・利用条件 |

空のリポジトリから始める手順、実習用ファイル、復元操作、.gitignore、理解度チェックは学習用の補足です。原資料の「分散型の代表例：GitHub」は「Git」に、pushの説明は「コミット済みの履歴の送信」に修正しました。pullの統合方式、mergeとrebaseのコミットに関する説明も、条件が分かる表現に整理しています。

画像は原資料の埋め込み画像を使用しています。`images/history.png` はスライド6などの `image2.png`、`images/diff.png` はスライド22の `image17.png` に対応します。Markdownとimagesフォルダを同じ場所に保つと画像を表示できます。

### 著作権・利用条件（公開用 Version 1.1 に準拠）

公開閲覧・非営利学習ライセンス Version 1.1

Copyright © 2026 Shinsuke Oouchi. All rights reserved.

制定：2026年8月30日／改定：2026年9月8日

**許可される利用**

- 著作権者または許可を受けた公開者による本公開版のオンライン掲載と一般閲覧
- 個人による私的かつ非営利の学習利用（公開リポジトリからの閲覧・保存を含む）
- ヒートウェーブ株式会社の授業に必要な範囲での教員による改変・追記、教員・受講者への複製・配布
- 当該授業内での投影・画面共有（本表示を保持）

**禁止される利用**

- 許可のない転載・再公開・公衆送信（SNS・動画・公開リポジトリ等への掲載を含む）
- 許可のない改変版の公開、授業参加者以外への再配布
- 販売・貸与・再許諾、営利・広告・宣伝目的の利用
- 著作権表示の削除・改変、第三者素材の抽出・再利用

**本公開版は著作権者自身による一般公開です。第三者による転載・再配布・改変版の公開には許可が必要です。**

第三者素材には各権利者の利用条件が適用されます。上記以外の利用には著作権者の事前の書面許可が必要です。

本資料は現状有姿で提供され、内容の正確性・完全性を保証しません。利用により生じた損害について著作権者は責任を負いません。

本利用条件は日本法に準拠します。公開の許可は、著作権の放棄や素材の自由利用を意味しません。
